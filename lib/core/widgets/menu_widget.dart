import 'package:flutter/material.dart';

class MeduWidget extends StatelessWidget {
  const MeduWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Menu");
  }
}